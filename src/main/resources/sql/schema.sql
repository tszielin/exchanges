CREATE TABLE USERS(
	ID INT GENERATED BY DEFAULT AS IDENTITY (START WITH 1, INCREMENT BY 1) NOT NULL,
	USERNAME VARCHAR(25) NOT NULL,
	PASSWORD VARCHAR(50) NOT NULL,
	FIRST_NAME VARCHAR(30) NOT NULL,
	LAST_NAME VARCHAR(50) NOT NULL,
	PHONE VARCHAR(15),
	EMAIL VARCHAR(100) NOT NULL,
	BIRTHDAY DATE NOT NULL,
	ACTIVE BOOLEAN DEFAULT TRUE NOT NULL,
	ADDRESS_ID INT NOT NULL,
	PRIMARY KEY(ID),
	UNIQUE (USERNAME)
);

CREATE TABLE ADDRESSES(
	ID INT GENERATED BY DEFAULT AS IDENTITY (START WITH 1, INCREMENT BY 1) NOT NULL,
	ADDRESS VARCHAR(150) NOT NULL,
	PO_BOX VARCHAR(10),
	TOWN VARCHAR(50) NOT NULL,
	COUNTRY VARCHAR(70) NOT NULL,
	PRIMARY KEY(ID)
);
ALTER TABLE USERS ADD CONSTRAINT FK_USERS_ADDRESS foreign key (ADDRESS_ID) REFERENCES ADDRESSES;

INSERT INTO ADDRESSES(ADDRESS,PO_BOX,TOWN,COUNTRY)
	VALUES ('Lubinska 2','53625','Wroclaw','Poland');
INSERT INTO USERS(USERNAME,PASSWORD,LAST_NAME,FIRST_NAME,EMAIL,BIRTHDAY,ADDRESS_ID)
	VALUES ('tszielin','tszielin','Zielinski','Thomas','tszielin@outlook.com','1969-07-05',1);
INSERT INTO ADDRESSES(ADDRESS,TOWN,COUNTRY)
	VALUES ('Demo street','Demo town','Demo country');
INSERT INTO USERS(USERNAME,PASSWORD,LAST_NAME,FIRST_NAME,EMAIL,BIRTHDAY,ADDRESS_ID)
	VALUES ('demo','demo','User','Demo','demo@demo.pl','1900-01-01',2);